// Arrays, objects and regerence type
// object and arry are called refrance type of data 
// push method will add to th excsting method 

// arrays and array method with map for objectes
const person  ={
    name:'jyoti',
    age: 36,
    greet(){
console.log("hi my name is " + this.name);
    }
};

const hobbies =['sports', 'cooking', 'driwing'];
hobbies.push('programing');// the push method edit the even constent method also just it want edit anything it add th objects
console.log(hobbies);
